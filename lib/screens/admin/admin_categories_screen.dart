import 'package:flutter/material.dart';

/// Placeholder — implementação real (lista de categorias com edição de
/// produtos embutida por categoria) é o item 4 do roadmap, ainda não
/// feito.
class AdminCategoriesScreen extends StatelessWidget {
  const AdminCategoriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Categorias — em construção'));
  }
}